#Creamos una clase nodo que almacenará el valor de cada sección de la lista y el apuntador a donde estará unido dicho elemento
class Nodo:
   def __init__(self, value):
      self.value = value
      self.next = None
      self.previous = None
class ListaEnlazada:
   def __init__(self):
      self.head = None
      self.tail = None
